name: Build NexaChat 3.0.2 Liquid Glass

on:
  push:
    branches: [main]
    paths:
      - "NexaChat-v3.0.2-glass-source.zip"
      - ".github/workflows/build-nexachat-3.0.2-glass.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: test-lint-apk
    runs-on: ubuntu-latest
    timeout-minutes: 55

    steps:
      - name: Download repository
        uses: actions/checkout@v4

      - name: Verify source archive
        shell: bash
        run: |
          set -euo pipefail
          test -f NexaChat-v3.0.2-glass-source.zip
          echo "4b0a0bf12e0549bb2198cf46a2cc6cd1a3043577177b87cba562cbb6d75e8e65  NexaChat-v3.0.2-glass-source.zip" | sha256sum --check --strict

      - name: Unpack NexaChat 3.0.2
        shell: bash
        run: |
          set -euo pipefail
          rm -rf /tmp/nexachat-302
          mkdir -p /tmp/nexachat-302
          unzip -q NexaChat-v3.0.2-glass-source.zip -d /tmp/nexachat-302

          # Defensive normalization in case an unzip implementation changes modes.
          chmod -R u+rwX,go+rX /tmp/nexachat-302/NexaChat-v3.0.2-glass

          cp -R /tmp/nexachat-302/NexaChat-v3.0.2-glass/. .
          test -f settings.gradle.kts
          test -f app/build.gradle.kts
          grep -nE 'compileSdk|targetSdk|versionName' app/build.gradle.kts

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK 36
        shell: bash
        run: |
          set -euo pipefail
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle cache
        uses: gradle/actions/setup-gradle@v4

      - name: Static verification
        shell: bash
        run: |
          chmod +x gradlew tools/verify-static.sh
          ./tools/verify-static.sh

      - name: Unit tests and debug APK
        shell: bash
        run: |
          set -o pipefail
          ./gradlew --no-daemon --stacktrace :app:testDebugUnitTest :app:assembleDebug 2>&1 | tee build.log
          cp app/build/outputs/apk/debug/app-debug.apk NexaChat-3.0.2-Liquid-Glass-debug.apk

      - name: Android lint
        shell: bash
        run: |
          set -o pipefail
          ./gradlew --no-daemon --stacktrace :app:lintDebug 2>&1 | tee -a build.log

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: NexaChat-3.0.2-Liquid-Glass-APK
          path: NexaChat-3.0.2-Liquid-Glass-debug.apk
          if-no-files-found: error
          retention-days: 21

      - name: Upload reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: NexaChat-3.0.2-build-reports
          path: |
            build.log
            app/build/reports/tests/testDebugUnitTest/**
            app/build/reports/lint-results-debug.html
            app/build/reports/lint-results-debug.xml
          if-no-files-found: ignore
          retention-days: 7
